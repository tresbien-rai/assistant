# Session Handoff — 2026-07-31

Streaming tool loop, session state, and a UI pass. Everything below is merged to
`main` (PRs #157–#168).

---

## 1. DONE — the live pass ran, and found two total-failure bugs

Both items this section used to list have been run against real keys, and each
turned out to be broken outright rather than subtly wrong. Both are fixed and
merged.

**#170 — Gemini's tool loop was a silent no-op.** `gemini.streamRaw` split SSE
frames on `'\n\n'`, but Google terminates SSE lines with CRLF: frames arrive
separated by `\r\n\r\n`, which contains no `\n\n` substring. No frame was ever
parsed, so `streamRaw` returned an EMPTY parts array — HTTP 200, no error, no
exception, nothing rendered, typing indicator simply vanished. Only tools-on
turns were affected (the toolless path is a dumb passthrough, and the frontend
already normalises CRLF), which since SS-03 is nearly every send.

**#171 — extended thinking 400'd on every send.** Anthropic rejects `top_k` and
`top_p` when thinking is enabled and requires temperature 1, but every model
profile ships `topK: 40` enabled. Ticking "extended thinking" on any model
therefore failed with ``top_k` must be unset when thinking is enabled`. Guard
now drops the forbidden params, mirroring the older temperature/top_p guard.

Results of the four checks this section originally specified:

| # | test | result |
|---|---|---|
| 1 | two or more tools in sequence | ✅ three rounds of text interleaved with two tool calls; separator correct |
| 2 | write a large file | ⚠️ **still not run** — needs Drive, and the dev-login account has no Drive token. Whole-`functionCall`-arg reassembly is covered indirectly (a write + targeted edit landed byte-exact) but the size dimension is untested |
| 3 | text streams progressively | ✅ multiple chunks over ~600 ms, not one lump |
| 4 | A/B against Anthropic | ✅ structurally indistinguishable |

Extended thinking + tools is now verified on both providers, including
thinking-signature preservation across rounds (the loop replays the signed
block into rounds 2 and 3 and Anthropic accepts it). Gemini works at
`level=low`, `level=high` and `budget=4000` with stock sampling params.

---

## 2. What shipped

**Streaming actually streams (TS-01…TS-05).** `/api/chat/stream` used to run the
tool loop NON-streaming and emit the whole answer as one synthetic chunk; the
real stream path was only reachable with zero tools advertised, and the
scratchpad auto-armed, so nearly every send took the non-streaming branch. Both
providers now stream every round via `streamRaw`, so pre-tool narration is
visible instead of discarded, and tool cards render inline at the point they ran
(`textOffset` on the attachment, converted between live and stored coordinate
spaces — see the comments in `send.js`/`thread.js` before touching it).

**Session state (SS-01…SS-04).** `docs/SESSION_STATE_DESIGN.md` is the reference.
The model is now told what exists AND what does not; the scratchpad is on by
default; tool descriptions no longer assert containers that may not exist; both
edit tools plus `write_scratchpad` report before → after deltas.

**UI.** One field/checkbox appearance app-wide (`styles/base.css`), one
resizable-textarea treatment, inline tool cards stack rather than scrolling
sideways, and streaming no longer fights the user's scroll.

---

## 3. Verified vs not

**Live-verified by the user:** streaming works; multi-tool turns work; inline
cards appear (layout since fixed in #168); scroll behaviour good; behaviour in
bare chats / fresh chats / after edits looked consistent.

**Also live-verified since (2026-07-31, second pass):** Gemini end to end after
#170 — progressive streaming, two-tools-in-sequence, inline cards landing where
they ran; extended thinking + tools on both providers after #171; and the **#168
layout fix, now confirmed by eye** rather than by measurement.

**Not verified live:**
- **Large file writes** (§1 test 2) — blocked on Drive, not on code.
- `write_scratchpad`'s new delta wording.
- **Intermittent, unresolved:** roughly one Gemini turn in twelve returned the
  model's internal tool-call syntax as *text* —
  `довжcall:default_api:write_scratchpad{...}` — with no tools running. Not
  reproducible across bare / +systemPrompt / full-UI payloads or repeats against
  a pre-populated pad. The Cyrillic fragments point at token-level generation
  rather than anything in the transport. Suspected generation-parameter quirk.
- One `level=high` Gemini run stalled >4 min on its final round; an identical
  retry finished in 10 s. Transient, not reproducible.

**Caveat that cost time this session:** the live pass reported bugs that were
already fixed, because the browser was serving **cached CSS**. `express.static`
sends ETags but no cache-busting, so a hard refresh is needed after a style
change. To check a specific rule rather than eyeballing:

```js
Array.from(document.styleSheets).flatMap(s=>{try{return Array.from(s.cssRules)}catch{return[]}})
  .filter(r=>r.selectorText?.includes('message-content:last-child')).map(r=>r.style.content)
```

`["▌"]` = current, `["u258C"]` = stale.

---

## 4. Open threads

**The ghost file — unresolved, deliberately parked.** A file
(`ode_to_tomatoes.txt`, 319 B, downloads scope) appeared in `list_files` that the
tester says they never created; it existed only in the scratchpad. Not
reproducible locally — **every file table in the local dev DB is empty**, so it
lives in the deployed environment.

What was ruled out by reading the code: `scratchpad.js` imports only config,
dal, diff, format and logger — it never touches Drive or any file store, and
every write into the downloads scope goes through `writeContentToStore`, whose
only callers are `create_file`, `edit_file`, the re-roll rollback, and
user-initiated saves. **There is no implicit scratchpad-to-file path.** So it was
an explicit tool call or predates the session. The 319 B is consistent with an
early short draft rather than the finished poem.

To settle it, `file_revisions` records `author`/`op`/`turn` per write:

```sql
SELECT r.author, r.op, r.turn, r.created_at, f.filename
FROM file_revisions r JOIN user_files f ON f.id = r.file_id
WHERE f.filename LIKE '%tomato%' ORDER BY r.created_at;
```

A model-authored row with a turn number closes it. **Rows absent while the file
exists is a real bug** and should be escalated.

**File digests (§7 of the design doc), specified but not built.** Three tiers
that compose: deterministic first-line/heading as a free floor; the model
writing a summary while it already has the content (a `summary` param on
create/edit, plus a batched `describe_files` for files injected by FC-03B); and
an aux model last. Provenance tagging (`authored` / `auto` / `none`) is the
load-bearing piece and should ship with tier 1. Tier 3 belongs with the eventual
global memory system rather than the file tools — store digests with
author/provenance/timestamp from day one so that system inherits something it
can reason about.

**Also outstanding:** prompt caching (none exists — no `cache_control` anywhere
in `server/src/`; §5 of the design doc covers the block split it would justify);
CF-02b (PDFs as working files); and `test-apiKeys-http.js`, which fails on clean
`main` because it needs a live server on `:3000` — a pre-existing integration
test, not a regression.

---

## 5. Gotchas worth knowing

- **Do not rewrite whole files with scripts.** A Python rewrite of `chat.css`
  normalised CRLF→LF across the file and put 2,700 lines of noise in one commit
  for a 3-line change. The Edit tool preserves line endings; use it.
- **`normalizeBlocks` appends missing preset blocks deliberately.** Slotting them
  in at their built-in index was tried and reverted: it breaks the stronger
  guarantee that the user's explicit order wins. `test-presets` catches it.
- **A test that accepts two shapes cannot detect a shape error.** The Gemini
  round-separator bug passed its first test because the test read the streamed
  text through either provider's shape. Both streaming tests now read only
  through their own.
- **A fixture that models the wrong wire shape can't either — and this has now
  happened twice.** The CRLF bug (#170) passed its unit test because the fixture
  joined frames with `\n\n`, a wire Google never sends. The extended-thinking
  400 (#171) had no live coverage at all. Both features were completely broken
  while their tests were green. When testing a provider transport, build the
  fixture from the provider's ACTUAL bytes, and run at least one real request
  before calling a provider path done.
- The scratchpad `state` preset block is **position-only and cannot be disabled**;
  the composer handles it above the enabled check so an already-stored
  `enabled:false` is ignored rather than obeyed.
